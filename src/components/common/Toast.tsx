// Superseded by @mantine/notifications (see src/state/ui-store.ts). Kept as a
// dead placeholder — not rendered anywhere — pending a later cleanup task
// that removes commons once all screens migrate.
export function Toast() {
  return null;
}
